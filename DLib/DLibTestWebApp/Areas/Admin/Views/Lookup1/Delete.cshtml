@model DLibTestWebApp.Data.tblkp_Lookup1

<p class="bg-warning"><i class="fa fa-warning"></i> Are you sure you want to delete this record <em>(@Model.value)</em> ? This <strong>can't</strong> be undone</p>

<button type="button" class="btn btn-default" data-dismiss="modal">No</button>
<a href="@Url.Action("DeleteConfirmed","Lookup1", new {@area = "Admin", id=@Model.id})" class="btn btn-primary">Yes</a>